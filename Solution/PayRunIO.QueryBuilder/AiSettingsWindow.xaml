<Window x:Class="PayRunIO.QueryBuilder.AiSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Query Builder - AI Settings"
        x:Name="AiSettingsWindow1"
        Height="330"
        Width="450"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="OpenAI Settings" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

            <TextBlock Text="API Key:"/>
            <PasswordBox x:Name="ApiKeyBox" PasswordChar="*" Margin="0,0,0,10"/>

            <TextBlock Text="Endpoint:"/>
            <TextBox x:Name="EndPointBox" Margin="0,0,0,10"/>

            <TextBlock Text="Model:"/>
            <TextBox x:Name="ModelBox" Margin="0,0,0,10"/>

            <TextBlock x:Name="StatusText" Foreground="Green" Margin="0,10,0,0" Visibility="Collapsed"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Click="OnSaveClick" Width="100" Margin="2,0"/>
            <Button Content="Close" Click="OnCloseClick" Width="100" Margin="2,0"/>
        </StackPanel>
    </Grid>
</Window>
