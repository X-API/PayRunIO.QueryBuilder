@inject NavigationManager Navigation

@* Reached when authorization fails during in-circuit navigation (the initial page request is
   handled by the cookie scheme's LoginPath redirect to /signin instead). *@
<SignInChooser ReturnUrl="@("/" + Navigation.ToBaseRelativePath(Navigation.Uri))" />
